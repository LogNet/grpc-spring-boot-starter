management:
  prometheus:
    metrics:
      export:
        enabled: false
  simple:
    metrics:
      export:
        enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    root: INFO

grpc:
  port: 0

