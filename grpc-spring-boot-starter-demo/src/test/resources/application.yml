management:
  metrics:
    export:
      prometheus:
        enabled: false
      simple:
        enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    root: INFO
