spring:
  main:
    allow-bean-definition-overriding: true
  jmx:
    enabled: false
  cloud:
    consul:
      discovery:
        enabled: true
    service-registry:
      auto-registration:
        enabled: true
  application:
    name: Demo

security:
  basic:
    enabled: false
management:
  security:
    enabled: false

grpc:
  enable-reflection: true