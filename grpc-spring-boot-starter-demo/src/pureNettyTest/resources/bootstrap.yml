spring:
  application:
    name: grpc-demo
  cloud:
    config:
      enabled: false
    consul:
      discovery:
        enabled: false
      enabled: false
    service-registry:
      auto-registration:
        enabled: false
embedded:
  keycloak:
    enabled: false
  containers:
    enabled: false

